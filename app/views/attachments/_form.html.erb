<%= content_for :headline do %>
<h1>Uploading New Attachment &rarr; <span><%=@customer.name %></span></h1>
<% end %>


<% form_for [@customer, @attachment], :html => { :multipart => true } do |f| %>
  <% if @attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
      <% @attachment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<%= select("attachment", "attachment_type", Attachment::ATTACHMENT_TYPES)%>

  <%= f.file_field :attachment %>
  <div class="actions">
    
	<input type="submit" class="btn btn-green big" value="Submit">
  </div>
<% end %>

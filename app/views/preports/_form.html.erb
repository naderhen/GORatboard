<%= form_for(@preport) do |f| %>
  <% if @preport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preport.errors.count, "error") %> prohibited this preport from being saved:</h2>

      <ul>
      <% @preport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :board_id %><br />
    <%= f.text_field :board_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
